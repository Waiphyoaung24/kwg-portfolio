---
import Layout from '@layouts/Layout.astro';
import HeroGradient from '@components/HeroGradient.tsx';

// Same address as the Footer; the hero is the front door for freelance work.
const email = 'nexuslab.dev.mm@gmail.com';

// Showcase gallery — placeholder imagery from Pexels per user reference.
// Replace src URLs with real work when available. The markup appends Pexels
// size params (w=1200): the originals are 12 MP and their decode stalled the
// entrance animation. `body` is the copy the card expands into (see the
// work-detail overlay below); placeholder like the images, replace together.
const gallery = [
  {
    src: 'https://images.pexels.com/photos/8467429/pexels-photo-8467429.jpeg',
    title: 'Ethereal Dreams',
    sub: 'Visual storytelling',
    body: 'A gallery site where the sequence of images sets the pace. Scroll drives the narrative and the type stays out of the way.',
  },
  {
    src: 'https://images.pexels.com/photos/33276110/pexels-photo-33276110.jpeg',
    title: 'Midnight Bloom',
    sub: 'Dark elegance',
    body: 'A dark editorial layout for a fashion label. Large imagery, sparse copy, and motion that never competes with the product.',
  },
  {
    src: 'https://images.pexels.com/photos/3378993/pexels-photo-3378993.jpeg',
    title: 'Golden Hour',
    sub: 'Warm minimalism',
    body: 'A launch page built around light and negative space. One idea per screen, one action at the end.',
  },
  {
    src: 'https://images.pexels.com/photos/1231258/pexels-photo-1231258.jpeg',
    title: 'Urban Solace',
    sub: 'City life reimagined',
    body: 'A city guide reworked as a scrolling map of moments. Photography leads and the interface follows.',
  },
];

// Capabilities — three "stands" with the scroll-stand 3D-card reveal.
// Heading words are pre-split server-side (no runtime SplitText) so the
// inline script can simply select `.word`s and animate their color.
const stands = [
  {
    heading: 'Websites & Landing Pages',
    sub: 'High-end sites built to perform and convert.',
    cards: [
      'https://i.pinimg.com/webp85/1200x/18/32/a0/1832a0151330ab6d294430e4514c8d62.webp',
      'https://i.pinimg.com/webp85/736x/48/b1/78/48b17830b4d8357cc77c00cc3b305d33.webp',
      'https://i.pinimg.com/736x/98/64/06/9864062eb69f36ababb1c7b9eafe9041.jpg',
      'https://i.pinimg.com/736x/91/26/3d/91263d9faaf8c0cb2dc6d0895cf9a4c9.jpg',
      'https://i.pinimg.com/webp85/1200x/f6/71/df/f671dfeb4fdcd20ce3f735856becd301.webp',
    ],
  },
  {
    heading: 'Brand & Identity',
    sub: 'Visual systems that feel intentional, memorable, and unmistakably yours.',
    cards: [
      'https://i.pinimg.com/736x/04/51/50/045150afa7b4c1cba03c820404a797da.jpg',
      'https://i.pinimg.com/webp85/1200x/1f/72/fa/1f72fa7b97455361a75ce32e9231a1e0.webp',
      'https://i.pinimg.com/webp85/736x/e7/67/ee/e767ee0e687bad29566e3572649ef158.webp',
      'https://i.pinimg.com/webp85/1200x/11/ad/d7/11add7639770b4af0d80fc196e6b047d.webp',
      'https://i.pinimg.com/736x/a9/d4/41/a9d441397c2182a4d338fce6e0b1a7a7.jpg',
    ],
  },
  {
    heading: 'Product & UI',
    sub: 'Interfaces crafted with clarity — every screen considered, every interaction refined.',
    cards: [
      'https://i.pinimg.com/webp85/1200x/e5/c3/7a/e5c37a81fd544d98e80575c046d8f11e.webp',
      'https://i.pinimg.com/736x/3d/2e/c1/3d2ec1c74d737e1318c85b0f1a7dbca2.jpg',
      'https://i.pinimg.com/736x/0c/41/bb/0c41bbd2138ece6834fc488bace34cc9.jpg',
      'https://i.pinimg.com/webp85/1200x/18/32/a0/1832a0151330ab6d294430e4514c8d62.webp',
      'https://i.pinimg.com/webp85/736x/48/b1/78/48b17830b4d8357cc77c00cc3b305d33.webp',
    ],
  },
];

// How I work — the process a stranger will live through. Four steps, no
// pricing (PRODUCT.md: no fabricated numbers). Edit the specifics here.
const steps = [
  {
    title: 'Discovery call',
    body: 'A short call. You describe the problem and what it costs you today. I ask questions. No pitch.',
  },
  {
    title: 'Scope and fixed quote',
    body: 'A written scope: what is in, what is out, one price, one timeline. You sign off before anything is built.',
  },
  {
    title: 'Build with weekly updates',
    body: 'Working software from the first weeks, not a reveal at the end. A written update every week.',
  },
  {
    title: 'Handover and support',
    body: 'Source, documentation, and deployment handed over. A support period after launch, agreed in the scope.',
  },
];
---

<Layout>
  <main>
    <section class="hero" aria-labelledby="hero-headline">
      <div class="hero__gradient" aria-hidden="true">
        <HeroGradient client:only="react" />
      </div>
      <p class="hero__index label" data-inview-manual>
        01 &mdash; Wai Phyo Aung
      </p>

      <div class="hero__center">
        <div class="hero__copy">
          <h1 id="hero-headline" class="hero__headline h2" data-inview-manual>
            I build websites and the internal systems behind them.
          </h1>
          <div class="hero__actions" data-inview-manual>
            <a
              class="pill-btn pill-btn--filled"
              href={`mailto:${email}?subject=New%20project`}
            >
              Email me <span aria-hidden="true">&#8599;&#65038;</span>
            </a>
            <a class="pill-btn" href="#work">See selected work</a>
          </div>
        </div>

        <article class="lumen card" data-inview-manual>
          <div class="lumen__light" aria-hidden="true">
            <div class="lumen__bloom lumen__bloom--far"></div>
            <div class="lumen__bloom lumen__bloom--near"></div>
            <div class="lumen__slit"></div>
            <div class="lumen__shade"></div>
          </div>
          <div class="lumen__icon" aria-hidden="true">
            <svg viewBox="0 0 32 32">
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M26.2636 20.4752L29.7034 6.2832H22.8238L26.2636 20.4752Z"
              ></path>
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M12.2636 6.2832L17.0473 26.0199H23.9568L19.1731 6.2832H12.2636Z"
              ></path>
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M8.61287 6.28336L1.70337 6.28373L6.48706 26.0203L13.3966 26.02L8.61287 6.28336Z"
              ></path>
            </svg>
          </div>
          <div class="lumen__bottom">
            <p class="lumen__title">Available for new projects</p>
            <p class="lumen__sub">Bangkok, Thailand</p>
            <button
              class="lumen__switch"
              type="button"
              role="switch"
              aria-checked="false"
            >
              <span class="lumen__track"><span class="lumen__handle"></span></span>
              <span class="micro">Lights on</span>
            </button>
          </div>
        </article>
      </div>

      <p class="hero__cue label" data-inview-manual>
        Scroll <span aria-hidden="true">&darr;</span> 2026
      </p>
    </section>

    <section id="work" class="container work" aria-labelledby="work-heading">
      <header class="work__header">
        <p class="label">02 &mdash; Selected Work</p>
        <h2 id="work-heading" class="work__title-block">
          Selected Work<br />
          <span class="work__years">2024 &mdash; 2026</span>
        </h2>
      </header>

      <ul class="gallery" role="list">
        {
          gallery.map(({ src, title, sub }) => (
            <li class="gallery__item">
              <figure class="gallery__figure">
                <img
                  class="gallery__img"
                  src={`${src}?auto=compress&cs=tinysrgb&w=1200`}
                  alt={title}
                  loading="lazy"
                  decoding="async"
                />
                <figcaption class="gallery__caption">
                  <p class="gallery__title">{title}</p>
                  <span class="gallery__sub">{sub}</span>
                </figcaption>
              </figure>
            </li>
          ))
        }
      </ul>
    </section>

    <section class="capabilities" aria-labelledby="capabilities-heading">
      <header class="container capabilities__header">
        <p class="label" data-inview>03 &mdash; Capabilities</p>
        <h2 id="capabilities-heading" class="visually-hidden">Capabilities</h2>
      </header>

      {
        stands.map(({ heading, sub, cards }) => {
          const words = heading.split(/\s+/);
          return (
            <article class="stand">
              <div class="stand__inner">
                <div class="stand__copy">
                  <h3 class="stand__heading">
                    {words.map((w, i) => (
                      <>
                        <span class="word">{w}</span>
                        {i < words.length - 1 ? ' ' : ''}
                      </>
                    ))}
                  </h3>
                  <p class="stand__sub">{sub}</p>
                </div>

                <div class="stand__cards">
                  <div class="stand__stage">
                    {cards.map((src) => (
                      <article class="stand__card">
                        <img src={src} alt="" loading="lazy" />
                      </article>
                    ))}
                  </div>
                </div>
              </div>
            </article>
          );
        })
      }
    </section>

    <section
      id="process"
      class="container process"
      aria-labelledby="process-heading"
    >
      <header class="process__header">
        <p class="label" data-inview>04 &mdash; How I work</p>
        <h2 id="process-heading" data-inview>
          What the next few months look like
        </h2>
      </header>

      <ol class="process__list" role="list">
        {
          steps.map(({ title, body }, i) => (
            <li
              class="process__step"
              data-inview
              style={`--delay: ${i * 0.08}s`}
            >
              <span class="process__index label">0{i + 1}</span>
              <h3 class="process__title">{title}</h3>
              <p class="process__body">{body}</p>
            </li>
          ))
        }
      </ol>
    </section>

    <!--
      Work detail: the overlay a Selected Work card expands into
      (src/scripts/workTransition.ts). Sits outside the hero, which scales on
      exit and would otherwise trap the fixed overlay. data-lenis-prevent lets
      the column scroll natively on short viewports while Lenis is frozen.
    -->
    <div
      class="work-detail"
      role="dialog"
      aria-modal="true"
      aria-label="Selected work"
      data-lenis-prevent
    >
      <div class="work-detail__backdrop" aria-hidden="true"></div>
      <figure class="work-detail__preview">
        <img alt="" />
      </figure>
      <div class="work-detail__column">
        <button class="work-detail__back pill-btn" type="button">
          <span aria-hidden="true">&larr;</span> Back
          <span class="micro" aria-hidden="true">Esc</span>
        </button>
        {
          gallery.map(({ title, sub, body }, i) => (
            <div class="work-detail__group" data-index={i}>
              <p class="work-detail__eyebrow label">
                0{i + 1} &mdash; {sub}
              </p>
              <h3 class="work-detail__title h2">{title}</h3>
              <p class="work-detail__body">{body}</p>
              <a class="pill-btn" href="/works">
                See all work <span aria-hidden="true">&#8599;&#65038;</span>
              </a>
            </div>
          ))
        }
      </div>
    </div>
  </main>
</Layout>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  // Importing scroll.ts ensures the Lenis ↔ ScrollTrigger bridge is wired
  // before our triggers are created.
  import '../scripts/scroll';

  const imgs = gsap.utils.toArray<HTMLImageElement>('.gallery__img');
  const isDesktop = window.innerWidth > 768;
  // Reduced motion: the tiles simply appear (PRODUCT.md a11y floor).
  const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;

  imgs.forEach((img, i) => {
    if (reduceMotion) return;
    gsap.set(img, {
      rotation: i % 2 === 0 ? -50 : 50,
      transformOrigin: 'center center',
      y: isDesktop ? 500 : 350,
      opacity: 0,
    });

    ScrollTrigger.create({
      trigger: img,
      start: isDesktop ? 'top 100%' : 'top 120%',
      once: true,
      onEnter: () => {
        gsap.to(img, {
          rotation: 0,
          y: 0,
          opacity: 1,
          duration: 0.8,
          // Entrances ease out (the --ease-out-expo token); an in-out start
          // reads as a stall on arrival.
          ease: 'expo.out',
          delay: isDesktop ? (i % 2 === 0 ? 0 : 0.25) : 0,
        });
      },
    });
  });
</script>

<script>
  // Selected Work cards expand into the work-detail overlay: the image
  // Flip-morphs into the preview, the copy reveals, Back/Esc reverse it.
  import { WorkTransition } from '../scripts/workTransition';

  const overlay = document.querySelector<HTMLElement>('.work-detail');
  if (overlay) {
    new WorkTransition({
      items: Array.from(
        document.querySelectorAll<HTMLElement>('.gallery__figure'),
      ),
      img: '.gallery__img',
      overlay,
    });
  }
</script>

<script>
  // Lumen card: lights itself shortly after load, the pen's "on" state without
  // making the visitor find the switch; the switch then toggles it.
  const card = document.querySelector<HTMLElement>('.lumen');
  const sw = card?.querySelector<HTMLButtonElement>('.lumen__switch');
  if (card && sw) {
    const set = (lit: boolean) => {
      card.classList.toggle('is-lit', lit);
      sw.setAttribute('aria-checked', String(lit));
    };
    const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (reduceMotion) set(true);
    else setTimeout(() => set(true), 800);
    sw.addEventListener('click', () =>
      set(sw.getAttribute('aria-checked') !== 'true'),
    );
  }
</script>

<script>
  // Zoom handoff, after Giana's "zoom scroll" (codepen.io/giana/pen/BabdgjB)
  // minus the scroll-snap, which Lenis fights. The hero scales up, blurs and
  // fades as it leaves; the Selected Work header zooms in from small behind
  // it. Scrubbed to scroll through the Lenis-driven ticker in scroll.ts.
  // Targets are wrappers, not the [data-inview*] children: those carry a CSS
  // transition on filter/opacity/transform that would lag a scrubbed tween.
  import { gsap } from 'gsap';
  import '../scripts/scroll';

  if (!matchMedia('(prefers-reduced-motion: reduce)').matches) {
    gsap
      .timeline({
        scrollTrigger: {
          trigger: '.hero',
          start: 'bottom bottom',
          end: 'bottom top',
          scrub: 1,
        },
      })
      .to('.hero', { scale: 1.5, opacity: 0, ease: 'power1.in' }, 0)
      // Blur only the type; blurring the WebGL canvas every frame is costly.
      .to('.hero__center', { filter: 'blur(16px)', ease: 'power1.in' }, 0);

    gsap.from('.work__header', {
      scale: 0.6,
      opacity: 0,
      filter: 'blur(16px)',
      transformOrigin: '0% 50%',
      ease: 'power1.out',
      scrollTrigger: {
        trigger: '.work__header',
        start: 'top bottom',
        end: 'top 40%',
        scrub: 1,
      },
    });
  }
</script>

<script>
  // Scroll-stand: 5 cards per stand start fanned in 3D and flatten into a
  // row as the section enters the viewport. Heading words stagger color
  // from white → muted. Scrubbed to scroll position via the Lenis-driven
  // GSAP ticker wired in src/scripts/scroll.ts.
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  import '../scripts/scroll';

  // Read live design tokens so palette tweaks in _vars.scss flow through.
  const css = getComputedStyle(document.documentElement);
  const cLight = css.getPropertyValue('--c-ink').trim() || '#ffffff';
  const cMuted = css.getPropertyValue('--c-mute').trim() || '#7d8187';

  // Per-card initial 3D state — center card is most vertical, edges fan out.
  const cardStart = [
    { rotateX: 58, rotateY: 18, y: 48 },
    { rotateX: 74, rotateY: 5, y: 32 },
    { rotateX: 90, rotateY: 0, y: 18 },
    { rotateX: 74, rotateY: -5, y: 32 },
    { rotateX: 58, rotateY: -18, y: 48 },
  ];

  document.querySelectorAll<HTMLElement>('.stand').forEach((section) => {
    const cards = section.querySelectorAll<HTMLElement>('.stand__card');
    const copy = section.querySelector<HTMLElement>('.stand__copy');
    const words = section.querySelectorAll<HTMLElement>(
      '.stand__heading .word',
    );

    cards.forEach((card, i) => {
      gsap.set(card, {
        ...cardStart[i % cardStart.length],
        transformOrigin: '50% 100%',
        transformPerspective: 1560,
      });
    });

    if (copy) gsap.set(copy, { y: -10 });
    gsap.set(words, { color: cLight });

    gsap
      .timeline({
        scrollTrigger: {
          trigger: section,
          start: 'top 40%',
          end: 'top 0%',
          scrub: 1,
        },
      })
      .to(
        cards,
        { rotateX: 0, rotateY: 0, y: 0, ease: 'power1.inOut', duration: 1 },
        0,
      )
      .to(copy, { y: 0 }, 0)
      .to(words, { color: cMuted, stagger: 0.2 }, 0.1);
  });
</script>

<style lang="scss">
  // HERO — the front door: who, what, where, one way to reach out.
  // Three rows: top label, centered headline + proof + pills, bottom cue.
  // No imagery; a mesh gradient in the canvas greys (HeroGradient) behind the type.
  // 4rem subtracts the Header's vertical footprint (padding-block 1.25rem × 2
  // + ~1rem label content ≈ 3.5rem, rounded to 4rem) so the cue sits inside
  // the viewport instead of below the fold.
  .hero {
    position: relative;
    isolation: isolate;
    display: grid;
    grid-template-rows: auto 1fr auto;
    min-height: calc(100svh - 4rem);
    // Matches .container so the hero lines up with the header above it.
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding: to-rem(24) var(--grid-margin) to-rem(40);
  }

  // Full-bleed: the hero is container-width, but the backdrop runs edge to
  // edge, under the type. It also reaches up behind the (transparent) Header:
  // 68px = its 44px rail + 12px padding either side.
  .hero__gradient {
    --header-h: #{to-rem(68)};
    position: absolute;
    inset: 0 auto;
    top: calc(-1 * var(--header-h));
    left: 50%;
    width: 100vw;
    height: calc(100% + var(--header-h));
    transform: translateX(-50%);
    z-index: -1;

    // Dark washes over the left half, where the copy sits, and the top strip,
    // where the header sits, so the white in the sphere blooms lower right and
    // the type keeps its contrast. A bottom wash dissolves the canvas into the
    // page so the hero has no hard edge against the next section.
    &::after {
      content: '';
      position: absolute;
      inset: 0;
      background:
        linear-gradient(0deg, var(--c-canvas) 0%, transparent 28%),
        linear-gradient(
          180deg,
          var(--c-canvas) 0%,
          color-mix(in srgb, var(--c-canvas) 60%, transparent) 12%,
          transparent 30%
        ),
        linear-gradient(
          90deg,
          var(--c-canvas) 0%,
          color-mix(in srgb, var(--c-canvas) 70%, transparent) 40%,
          transparent 75%
        );
    }
  }

  // Body, not mute: the gradient lifts the backdrop past what mute can sit on.
  .hero__index {
    color: var(--c-body);
  }

  // Copy left, illumine card right; the card drops under the pills on mobile.
  .hero__center {
    display: grid;
    align-items: center;
    gap: clamp-fluid(40, 80);

    @media (min-width: 769px) {
      grid-template-columns: 1fr auto;
    }
  }

  .hero__copy {
    display: flex;
    flex-direction: column;
    gap: clamp-fluid(20, 40);
  }

  // LUMEN CARD — after RAFA3L's "Card Illumine" (codepen.io/RAFA3L/pen/ZYbabEa,
  // inspo Camden). Rebuilt on the .card primitive: hairlines carry elevation,
  // so the pen's stacked shadows go; the switch is a pill. The light itself is
  // illustration, the one place the system draws glow, so its geometry below
  // is raw values. `.is-lit` toggles it; the inline script flips it on after
  // load and wires the switch.
  .lumen {
    --t: 0.4s var(--ease-smooth);
    position: relative;
    // Grey falloff from the top, as the pen: the mid canvas grey fading into
    // the card grey.
    background: radial-gradient(
      circle at 50% 0%,
      var(--c-canvas-mid),
      var(--c-canvas-card) 64%
    );
    width: min(100%, #{to-rem(300)});
    aspect-ratio: 3 / 4;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    transition: translate var(--t);

    &:hover {
      translate: 0 #{to-rem(-3)};
    }
  }

  // Corner brackets: a hairline frame outside the card, clipped to its
  // corners, that tightens and widens on hover.
  .lumen::before {
    content: '';
    position: absolute;
    --ax: #{to-rem(56)};
    inset: to-rem(-12);
    border: 1px solid var(--c-hairline-bright);
    border-radius: var(--r-sm);
    pointer-events: none;
    clip-path: polygon(
      var(--ax) 0,
      0 0,
      0 var(--ax),
      var(--ax) var(--ax),
      var(--ax) calc(100% - var(--ax)),
      0 calc(100% - var(--ax)),
      0 100%,
      var(--ax) 100%,
      var(--ax) calc(100% - var(--ax)),
      calc(100% - var(--ax)) calc(100% - var(--ax)),
      calc(100% - var(--ax)) 100%,
      100% 100%,
      100% calc(100% - var(--ax)),
      calc(100% - var(--ax)) calc(100% - var(--ax)),
      calc(100% - var(--ax)) var(--ax),
      100% var(--ax),
      100% 0,
      calc(100% - var(--ax)) 0,
      calc(100% - var(--ax)) var(--ax),
      var(--ax) var(--ax)
    );
    transition:
      inset var(--t),
      clip-path var(--t);
  }

  .lumen:hover::before {
    --ax: #{to-rem(100)};
    inset: to-rem(-6);
  }

  .lumen__light {
    position: absolute;
    inset: 0;
    overflow: hidden;
    border-radius: inherit;
    pointer-events: none;
    perspective: 400px;
    // Vanishing point on the slit so its tilt foreshortens symmetrically.
    perspective-origin: 50% 50%;

    > * {
      position: absolute;
      left: 50%;
      translate: -50%;
      transition: opacity var(--t);
    }
  }

  .lumen__slit {
    top: 50%;
    width: 64%;
    height: to-rem(12);
    transform: rotateX(-76deg);
    background: var(--c-canvas);
    box-shadow: 0 0 12px 1px transparent;
    transition:
      background var(--t),
      box-shadow var(--t);
  }

  .is-lit .lumen__slit {
    background: var(--c-ink);
    box-shadow: 0 0 12px 1px color-mix(in srgb, var(--c-ink) 70%, transparent);
  }

  // Light spill as half-ellipses anchored on the slit: a hot core close in,
  // a wide soft wash further out. Dark until lit.
  .lumen__bloom {
    bottom: calc(50% - #{to-rem(6)});
    opacity: 0;
  }

  .is-lit .lumen__bloom {
    opacity: 1;
  }

  .lumen__bloom--far {
    width: 120%;
    height: 60%;
    background: radial-gradient(
      ellipse 50% 100% at 50% 100%,
      color-mix(in srgb, var(--c-ink) 28%, transparent),
      transparent 70%
    );
    filter: blur(24px);
  }

  .lumen__bloom--near {
    width: 74%;
    height: 34%;
    background: radial-gradient(
      ellipse 55% 100% at 50% 100%,
      color-mix(in srgb, var(--c-ink) 60%, transparent),
      transparent 70%
    );
    filter: blur(12px);
  }

  .lumen__shade {
    top: calc(50% + #{to-rem(6)});
    width: 80%;
    height: 40%;
    background: radial-gradient(
      ellipse 50% 100% at 50% 0,
      color-mix(in srgb, var(--c-canvas) 90%, transparent),
      transparent 70%
    );
    filter: blur(16px);
    opacity: 0.4;
  }

  .is-lit .lumen__shade {
    opacity: 1;
  }

  // The W mark in a hairline disc, brightening when lit.
  .lumen__icon {
    position: absolute;
    top: 18%;
    left: 50%;
    translate: -50%;
    width: to-rem(56);
    height: to-rem(56);
    display: grid;
    place-items: center;
    border: 1px solid var(--c-hairline);
    border-radius: var(--r-pill);
    color: var(--c-body);
    transition: color var(--t);

    svg {
      width: to-rem(28);
      height: to-rem(28);
      fill: currentColor;
    }
  }

  .is-lit .lumen__icon {
    color: var(--c-ink);
  }

  .lumen__bottom {
    position: relative;
    display: grid;
    grid-template-columns: 1fr auto;
    grid-template-areas:
      'title title'
      'sub switch';
    align-items: end;
    column-gap: var(--sp-md);
    row-gap: var(--sp-md);
  }

  .lumen__title {
    grid-area: title;
    margin: 0;
    font-size: var(--fs-5);
    letter-spacing: var(--ls-5);
    line-height: 1.3;
    color: var(--c-ink);
  }

  .lumen__sub {
    grid-area: sub;
    margin: 0;
    padding-bottom: var(--sp-sm);
    border-bottom: 1px solid var(--c-hairline);
    font-size: var(--fs-body-sm);
    white-space: nowrap;
    color: var(--c-body);
  }

  // Pill switch: outline track, ink handle that slides when lit.
  .lumen__switch {
    grid-area: switch;
    display: inline-flex;
    align-items: center;
    gap: var(--sp-sm);
    padding: 0;
    border: 0;
    background: none;
    color: var(--c-body);
    white-space: nowrap;
    cursor: pointer;

    &:focus-visible {
      outline: 1px solid var(--c-ink);
      outline-offset: to-rem(4);
      border-radius: var(--r-pill);
    }
  }

  .lumen__track {
    position: relative;
    width: to-rem(40);
    height: to-rem(22);
    border: 1px solid var(--c-hairline-bright);
    border-radius: var(--r-pill);
    transition: border-color var(--t);
  }

  .lumen__handle {
    position: absolute;
    top: to-rem(3);
    left: to-rem(3);
    width: to-rem(14);
    height: to-rem(14);
    border-radius: var(--r-pill);
    background: var(--c-body);
    transition:
      translate var(--t),
      background var(--t);
  }

  .lumen__switch[aria-checked='true'] {
    color: var(--c-ink);

    .lumen__track {
      border-color: var(--c-ink);
    }

    .lumen__handle {
      translate: to-rem(18);
      background: var(--c-ink);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .lumen,
    .lumen::before,
    .lumen__light > *,
    .lumen__slit,
    .lumen__icon,
    .lumen__track,
    .lumen__handle {
      transition: none;
    }
  }

  .hero__headline {
    margin: 0;
    max-width: 16ch;
    color: var(--c-ink);
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-md);
    margin-top: var(--sp-sm);
  }

  .hero__cue {
    justify-self: end;
    align-self: end;
    color: var(--c-body);
    display: inline-flex;
    align-items: center;
    gap: to-rem(8);
  }

  // SELECTED WORK — title block only. Cards intentionally absent (curtainsjs
  // alignment bug, see Canvas.astro). Holds the page section anchor for
  // future content.
  .work {
    // The hero is positioned and scales to 1.5x on exit (see zoom handoff
    // script); without a stacking order it would paint over this section.
    position: relative;
    z-index: 1;
    margin-block: clamp-fluid(80, 160) clamp-fluid(80, 160);
    display: flex;
    flex-direction: column;
    gap: clamp-fluid(40, 80);
  }

  .work__header {
    display: flex;
    flex-direction: column;
    gap: to-rem(16);
  }

  .work__header .label {
    color: var(--c-mute);
  }

  .work__title-block {
    color: var(--c-ink);
  }

  .work__years {
    color: var(--c-mute);
  }

  // GALLERY — 2-col on desktop, 1-col on mobile. Each tile is animated in
  // via the inline <script> at the bottom of this file (GSAP ScrollTrigger).
  // Initial state (rotated, translated, hidden) is applied in JS to avoid
  // a flash of un-animated content before GSAP's first set() runs; ensure
  // gsap.set fires before paint by importing in a module script.
  .gallery {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: clamp-fluid(32, 64);
  }

  .gallery__item {
    width: 100%;

    @media (min-width: 769px) {
      // calc(50% - half-gap) so two items fit per row with the flex gap.
      width: calc(50% - clamp-fluid(16, 32));
    }

    // Off-row offset reinforces the staggered, editorial feel from the
    // ScrollTrigger entrance.
    @media (min-width: 769px) {
      &:nth-child(even) {
        margin-top: clamp-fluid(40, 80);
      }
    }
  }

  // The figure is the click target (role=button from workTransition.ts), not
  // the <li>, so the list keeps its semantics.
  .gallery__figure {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: to-rem(12);
    cursor: pointer;

    &:focus-visible {
      outline: 1px solid var(--c-ink);
      outline-offset: to-rem(4);
    }
  }

  .gallery__img {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    background-color: var(--c-canvas-card);
    display: block;
    // GSAP-driven entrance, no CSS transition.
    will-change: transform, opacity;
  }

  .gallery__caption {
    display: flex;
    flex-direction: column;
    gap: to-rem(4);
  }

  .gallery__title {
    color: var(--c-ink);
    letter-spacing: 0;
    font-size: clamp-fluid(14, 16);
    font-weight: var(--fw-regular);
    margin: 0;
  }

  .gallery__sub {
    color: var(--c-body);
    font-size: clamp-fluid(12, 14);
  }

  // WORK DETAIL — the overlay a gallery card expands into, after Codrops'
  // image-to-content transition (see src/scripts/workTransition.ts). Hidden
  // until opened. The backdrop is the canvas fading over the page; the
  // preview is where the clicked image lands, full height on the left and
  // square like the tiles, capped at half the viewport so the copy keeps a
  // column. Stacks under the sticky header (10): the script fades the header
  // with the backdrop, and keeping it on top means it never pops in front of
  // the image on the last frame of the close.
  .work-detail {
    --preview-w: min(100vh, 50vw);
    display: none;
    position: fixed;
    inset: 0;
    z-index: 9;
    overflow-y: auto;
    overscroll-behavior: contain;

    @media (max-width: 768px) {
      --preview-w: 100vw;
    }
  }

  .work-detail__backdrop {
    position: fixed;
    inset: 0;
    background: var(--c-canvas);
  }

  .work-detail__preview {
    position: fixed;
    top: 0;
    left: 0;
    margin: 0;
    width: var(--preview-w);
    height: 100vh;
    overflow: hidden;
    background: var(--c-canvas-card);

    img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    @media (max-width: 768px) {
      height: 50vh;
    }
  }

  .work-detail__column {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--sp-2xl);
    padding: var(--sp-xl) var(--grid-margin) var(--sp-xl)
      calc(var(--preview-w) + var(--sp-4xl));

    @media (max-width: 768px) {
      padding: calc(50vh + var(--sp-xl)) var(--grid-margin) var(--sp-xl);
    }
  }

  .work-detail__back .micro {
    color: var(--c-mute);
  }

  // One group per card; the script marks the open one active.
  .work-detail__group {
    display: none;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--sp-lg);
    margin-block: auto;
    padding-block: var(--sp-2xl);

    &.is-active {
      display: flex;
    }
  }

  .work-detail__eyebrow {
    margin: 0;
    color: var(--c-mute);
  }

  .work-detail__title {
    margin: 0;
    color: var(--c-ink);
  }

  .work-detail__body {
    margin: 0;
    max-width: 40ch;
    color: var(--c-body);
    font-size: var(--fs-body-lg);
    line-height: 1.56;
  }

  .work-detail__group .pill-btn {
    margin-top: var(--sp-sm);
  }

  // CAPABILITIES — 3 "stand" sections where 5 cards are fanned in 3D
  // perspective and flatten into a row as you scroll. Word color staggers
  // from white → muted gray across each heading reveal.
  .capabilities {
    margin-block: clamp-fluid(120, 200);
  }

  .capabilities__header {
    margin-bottom: clamp-fluid(40, 80);

    .label {
      color: var(--c-mute);
    }
  }

  .stand {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    padding: clamp-fluid(80, 160) clamp-fluid(20, 64);
  }

  .stand__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
  }

  .stand__copy {
    z-index: 2;
    width: 100%;
    max-width: 36rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: to-rem(20);
    text-align: center;
  }

  .stand__heading {
    font-size: clamp-fluid(40, 80);
    font-weight: var(--fw-regular);
    line-height: 1;
    letter-spacing: var(--ls-2);
    text-align: center;
    margin: 0;
  }

  .stand__heading .word {
    display: inline-block;
    // GSAP animates this from --c-ink → --c-mute with stagger
    // as the stand scrolls into view. Static fallback if JS is disabled.
    color: var(--c-mute);
  }

  .stand__sub {
    font-size: clamp-fluid(14, 16);
    color: var(--c-body);
    max-width: 36ch;
  }

  .stand__cards {
    width: 100%;
    margin-top: clamp-fluid(24, 48);
    display: flex;
    justify-content: center;
    pointer-events: none;
  }

  .stand__stage {
    perspective: 1560px;
    perspective-origin: 50% 100%;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    gap: 0.5vw;
    transform-style: preserve-3d;
    width: 100%;
  }

  .stand__card {
    --card-w: 17vw;
    --card-h: 22vw;

    width: var(--card-w);
    height: var(--card-h);
    flex-shrink: 0;
    transform-origin: 50% 100%;
    transform-style: preserve-3d;
    border-radius: var(--r-sm); // sharp corners per DESIGN.md §7
    overflow: hidden;
    background-color: var(--c-canvas-card);
    will-change: transform;

    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
  }

  @media (max-width: 991px) {
    .stand__card {
      --card-w: 28vw;
      --card-h: 36vw;
    }
  }

  // HOW I WORK — four steps on hairline rules. Text only; the process is the
  // proof for a nervous stranger (PRODUCT.md: trust from prospective clients).
  .process {
    margin-block: clamp-fluid(80, 160);
    display: flex;
    flex-direction: column;
    gap: clamp-fluid(40, 80);
  }

  .process__header {
    display: flex;
    flex-direction: column;
    gap: to-rem(16);

    .label {
      color: var(--c-mute);
    }

    h2 {
      max-width: 18ch;
      color: var(--c-ink);
    }
  }

  .process__list {
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--c-hairline);
  }

  .process__step {
    display: grid;
    grid-template-columns: to-rem(64) 1fr;
    grid-template-areas:
      'index title'
      'index body';
    column-gap: var(--grid-gutter);
    row-gap: var(--sp-sm);
    padding-block: var(--sp-xl);
    border-bottom: 1px solid var(--c-hairline);

    @media (min-width: 769px) {
      grid-template-columns: to-rem(64) 1fr 1fr;
      grid-template-areas: 'index title body';
      align-items: baseline;
    }
  }

  .process__index {
    grid-area: index;
    color: var(--c-mute);
  }

  .process__title {
    grid-area: title;
    margin: 0;
    font-size: var(--fs-5);
    letter-spacing: var(--ls-5);
    color: var(--c-ink);
  }

  .process__body {
    grid-area: body;
    margin: 0;
    max-width: 44ch;
    color: var(--c-body);
    font-size: clamp-fluid(14, 16);
    line-height: 1.56;
  }
</style>
